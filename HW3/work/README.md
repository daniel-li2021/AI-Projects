# HW3: POMDP Temporal Reasoning

Run `my_solution3.py` with the sample input files in this directory:

```bash
python3 my_solution3.py
```

Expected output for the sample case: `states.txt` with `["S2", "S2", "S2", "S1"]`.

Sample files (`state_weights.txt`, etc.) are from the assignment PDF for local testing.
